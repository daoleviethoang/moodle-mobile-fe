import 'package:flutter/material.dart';

class MultiChoiceQuiz extends StatelessWidget {
  const MultiChoiceQuiz({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("multi choice");
  }
}
