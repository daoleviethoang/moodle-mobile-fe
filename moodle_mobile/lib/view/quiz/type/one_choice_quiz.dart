import 'package:flutter/material.dart';

class OneChoiceQuiz extends StatelessWidget {
  const OneChoiceQuiz({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("one choice");
  }
}
